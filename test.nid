#PREALLOCSTART=500
#PREALLOCEND=511

void main() {
  int x = 100;
  int y = 100;

  move_to(x, 500);
  move_to(y, 501)

  asm {
    mvix, 500
    mviy, 501
  }

  x = x + 50;
  y = y + 50;

  sleep(500);


  asm {
    mvix, 500
    mviy, 501
  }

  x = x + 50;
  y = y + 50;

  sleep(500);

  asm {
    mvix, 500
    mviy, 501
  }

  x = x + 50;
  y = y + 50;

  sleep(500);
  
  asm {
    mvix, 500
    mviy, 501
  }

  x = x + 50;
  y = y + 50;

  sleep(500);
}
